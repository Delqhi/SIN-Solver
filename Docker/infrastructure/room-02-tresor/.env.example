# room-02-tresor-vault Configuration (.env.example)
# 🚨 SECURITY CRITICAL: This file manages ALL system secrets
# DO NOT commit actual .env file with secrets to git!

# Vault Core Configuration
VAULT_DEV_ROOT_TOKEN_ID=${VAULT_ROOT_TOKEN}
VAULT_DEV_LISTEN_ADDRESS=0.0.0.0:8200
VAULT_LOG_LEVEL=info
VAULT_PORT=8200

# PostgreSQL Master Credentials
# From room-03-postgres-master (MUST be same as postgres .env)
POSTGRES_PASSWORD=${VAULT_POSTGRES_PASSWORD}

# Redis Cache Credentials
# From room-04-redis-cache (MUST be same as redis .env)
REDIS_PASSWORD=${VAULT_REDIS_PASSWORD}

# n8n Security Keys (must be strong random strings)
# Generate new keys: openssl rand -hex 32
N8N_ENCRYPTION_KEY=${VAULT_N8N_ENCRYPTION_KEY}
N8N_USER_MANAGEMENT_JWT_SECRET=${VAULT_N8N_JWT_SECRET}

# n8n External Integrations
SENDGRID_API_KEY=${VAULT_SENDGRID_API_KEY}
SENDGRID_FROM_EMAIL=${VAULT_SENDGRID_FROM_EMAIL}
SLACK_API_TOKEN=${VAULT_SLACK_API_TOKEN}

# OpenCode AI Integration
OPENCODE_API_KEY=${VAULT_OPENCODE_API_KEY}
