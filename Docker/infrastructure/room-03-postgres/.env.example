# PostgreSQL Master Database Configuration (.env.example)
# 🚨 IMPORTANT SECURITY NOTICE:
# This is a TEMPLATE file showing required variables.
# DO NOT commit actual .env file with secrets to git!
# All secrets must come from room-02-tresor-vault

# Database Credentials (from vault)
DB_USER=postgres
DB_PASSWORD=${VAULT_POSTGRES_PASSWORD}
DB_NAME=sin_solver
DB_PORT=5432
DB_HOST=room-03-postgres-master

# Backup Settings
BACKUP_ENABLED=true
BACKUP_SCHEDULE="0 2 * * *"
BACKUP_RETENTION_DAYS=30

# Performance Tuning
POSTGRES_MAX_CONNECTIONS=200
POSTGRES_SHARED_BUFFERS=256MB
POSTGRES_EFFECTIVE_CACHE_SIZE=1GB

# Logging
POSTGRES_LOG_STATEMENT=all
POSTGRES_LOG_MIN_DURATION_STATEMENT=100
