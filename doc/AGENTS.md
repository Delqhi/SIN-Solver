# 🚀 AGENTS.MD - CEO PRODUCTION MANDATE 2026 (V8.0)

**Status:** GLOBAL MANDATE (Enterprise Browser Automation)  
**Version:** 8.0 "CEO MASTER" (UPDATED 2026-01-24 06:15)  
**Architecture:** STEEL BROWSER + VERCEL RUST CLI + DUAL-VISION BRAIN

---

## 🧠 THE "ULTIMATE" PHILOSOPHY (V8.0)

**MANDATE: All browser automation MUST utilize the Steel + Vercel + Vision synergy.**  
We have eliminated legacy Python overhead. We use native Rust speed and containerized stealth.

### Why this Stack?
- **Engine: Steel Browser (Zimmer-05)**: The ultimate stealth motor. Native CDP connection ensures 100% bypass of advanced bot-mitigation (Cloudflare, Akamai).
- **Courier: Vercel Agent-Browser (Rust CLI)**: 10x faster snapshots and interactions than any Python-based script. The flink courier for our engine.
- **Brain: Gemini 3 Pro + Mistral Vision**: Multi-brain consensus for pixel-perfect CAPTCHA solving and visual grounding.

### Deprecated Methods (PURGED)
- ❌ **browser-use** (Too slow, version conflicts, legacy overhead)
- ❌ **playwright-direct** (Lacks enterprise-grade stealth features of Steel)
- ❌ **selenium-python** (Museum-grade legacy)

---

## 🏗️ PRODUCTION ARCHITECTURE V8.0

```
┌─────────────────────────────────────────────────────────────┐
│               CEO MASTER ORCHESTRATOR                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────┐      ┌─────────────────────────────┐  │
│  │ Vision AI Brain  │ ◄──► │  Vercel Agent-Browser (CLI) │  │
│  │ (Gemini + Mistral)│      │  (The Speed Courier)        │  │
│  └──────────────────┘      └────────────┬────────────────┘  │
│                                         │                   │
│                                         ▼                   │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              STEEL BROWSER ENGINE (Zimmer-05)          │  │
│  │    (Native CDP + Containerized Stealth + Biometrics)   │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📂 CORE PRODUCTION TOOLS

### 1. Steel Precision Controller
The native interface to our engine. Uses `playwright.chromium.connect_over_cdp` to orchestrate missions with zero footprint.

### 2. Vercel Agent-Browser (Rust Courier)
```bash
# Snapshot Action (Ultra-Fast)
agent-browser snapshot -i --json

# Precision Action
agent-browser click @ref
```

### 3. Advanced Solver Service
Specialized AI logic for complex interactive puzzles:
- **Click-Order**: Sequence detection via Vision AI.
- **Slider**: High-precision offset calculation.
- **Rotate**: Visual orientation correction.

---

## 🛡️ BEST PRACTICES (CEO LEVEL 2026)

### 1. Speed Synergy
Always use Vercel CLI for snapshots. Use Python only for complex biometric calculations.
```python
# CORRECT: Use the speed courier
snapshot = await controller.get_vercel_snapshot()

# CORRECT: Use the stealth engine for biometric clicks
await controller.human_mouse_move(x, y)
```

### 2. Radical Cleanup
Never ship legacy files. `browser-use` is banned from production environments. Keep the root clean. Keep the code focused.

### 3. Visual Evidence (CEO Standard)
Capture high-quality screenshots via Vercel for every critical step and upload them to our visual asset hosting for real-time monitoring.

---

## 🚨 COMPLIANCE & PERFORMANCE

- **Startup Latency:** < 50ms (Native CDP)
- **Snapshot Speed:** < 100ms (Rust-powered)
- **Accuracy Rate:** 99.9% (Multi-brain Consensus)
- **Permission Mandate:** `"permission": "allow"` must NEVER be changed.

---
*"We don't build software to solve problems; we build engines to dominate them."*
